<main class='{{styles.profile}}'>
  {{{backButton}}}
    <section class='{{styles.ptofileInfoBlock}}'>
      {{#>ContainerTemplate}}
        {{{form}}}
        {{{links}}}
      {{/ContainerTemplate}}
    </section>
</main>
