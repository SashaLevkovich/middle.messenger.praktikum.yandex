<main class='{{styles.profile}}'>
  {{{backButton}}}
    <section class='{{styles.profileInfoBlock}}'>
      {{#>ContainerTemplate}}
        {{{form}}}
        {{{links}}}
      {{/ContainerTemplate}}
    </section>
</main>
