<header class='{{styles.dialogHeader}}'>
  <div class='{{styles.dialogContainer}}'>
    <span class='{{styles.userName}}'>
      {{userName}}
    </span>

    <form class='{{styles.addChat}}' id='addUser'>
      {{{input}}}
      {{{addUser}}}
      {{{deleteUser}}}
    </form>
  </div>
</header>
